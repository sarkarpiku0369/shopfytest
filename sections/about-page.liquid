{%- if section.settings.about-title -%}
    <div> {{section.settings.about-title}} </div>
{%- endif -%}

{% schema %}
{
    "name": "Section Name",
    "settings" :[
        {
            "type":"text",
            "id":"about-title",
            "label" : "About Title",
            "default":"About US"
        }
    ]
}
{% endschema %}